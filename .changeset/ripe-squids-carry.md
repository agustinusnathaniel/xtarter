---
'xtarterize': minor
---

Enhanced `doctor` command with grouped output (Environment, Tools, Project, Configuration), environment and project health checks, installed tool version display, and `--verbose` flag for system information.
