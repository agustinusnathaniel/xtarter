---
title: Getting Started with create-xtarter-app
description: Scaffold a new project from a curated template.
---

import { LinkButton } from '@astrojs/starlight/components'
import AllPackageManagers from '@/components/AllPackageManagers.astro'

`create-xtarter-app` scaffolds a new project from a curated template, installs its dependencies, and can initialize Git.

## Quick start

<AllPackageManagers type="create" pkg="xtarter-app@latest" />

The CLI prompts for a project name, template, package manager, Git init, and CI/CD cleanup. Use `--yes` / `-y` to accept the defaults (pnpm, `next-chakra`, Git init, no cleanup); the project name prompt still appears unless you pass a positional name. Explicit flags such as `--pm bun` or `--clean` take priority.

<AllPackageManagers type="create" pkg="xtarter-app@latest" args="my-app --yes" title="Scaffold with defaults" />

<AllPackageManagers type="create" pkg="xtarter-app@latest" args="my-app -t vite-tailwind" title="Pick a template" />

<LinkButton href="/create-xtarter-app/guide/templates/" size="lg">Browse all templates</LinkButton>

## Next steps

- [Usage guide](/create-xtarter-app/getting-started/usage/) - Walk through each prompt
- [CLI reference](/create-xtarter-app/guide/cli/) - Commands, arguments, and flags
- [Template catalog](/create-xtarter-app/guide/templates/) - Every template
- [Keep in sync](/xtarterize/) - Maintain configs after scaffolding
