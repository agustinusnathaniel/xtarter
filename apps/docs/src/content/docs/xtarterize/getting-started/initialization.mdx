---
title: Initialization
description: Apply conformance configuration to a project.
---

import { Steps } from '@astrojs/starlight/components'
import AllPackageManagers from '@/components/AllPackageManagers.astro'

## Initialize a project

<AllPackageManagers type="exec" pkg="xtarterize@latest" args="init" />

<Steps>

1. **Scan** the project for framework, bundler, package manager, monorepo status, and existing configs
2. **Resolve** which tasks apply to the detected stack
3. **Check** each task's status: `new`, `patch`, `skip`, or `conflict`
4. **Plan** the changes in a conformance table
5. **Prompt** to apply all, select tasks, dry-run, or quit

</Steps>

## Options

See the [CLI reference](/xtarterize/guide/cli/overview/#init) for every `init` flag.

## Examples

<AllPackageManagers type="exec" pkg="xtarterize@latest" args="init --dry-run" title="Preview" />

<AllPackageManagers type="exec" pkg="xtarterize@latest" args="init --yes" title="Apply all" />

<AllPackageManagers type="exec" pkg="xtarterize@latest" args="init --skip codegen/plop,agent/skills-install" title="Skip tasks" />

<AllPackageManagers type="exec" pkg="xtarterize@latest" args="init --only lint/biome" title="Only specific" />

## Monorepo projects

Which tasks are offered depends on where you run. See the [monorepo guide](/xtarterize/guide/monorepo/) for root and package scopes.

## What it can apply

`init` applies only tasks that match the detected project. See the [task catalog](/xtarterize/guide/tasks/overview/) for task IDs and applicability.

:::note
A task that is already conformant is skipped, so running `init` twice produces no changes on the second run.
:::

## Ultracite

[Ultracite](https://ultracite.ai/) is installed alongside any lint task (Biome, Oxlint, or Oxfmt), and generated configs extend its presets. xtarterize overlays its own conventions, such as kebab-case filenames and single quotes; customize by editing the generated config files.
