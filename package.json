{
	"name": "xtarter",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"contributors:add": "all-contributors add",
		"contributors:generate": "all-contributors generate",
		"contributors:check": "all-contributors check",
		"build": "turbo run build",
		"verify:shebangs": "node scripts/verify-shebangs.mjs",
		"postbuild": "pnpm verify:shebangs",
		"build:ci": "turbo run build --filter=!@xtarterize/docs",
		"postbuild:ci": "pnpm verify:shebangs",
		"dev": "turbo run dev",
		"test": "vp test",
		"lint": "biome check",
		"lint:fix": "biome check --write",
		"typecheck": "turbo run typecheck",
		"check": "turbo run typecheck build lint test verify:shebangs",
		"check:ci": "turbo run typecheck build --filter=!@xtarterize/docs lint test verify:shebangs",
		"docs:dev": "pnpm --filter @xtarterize/docs dev",
		"docs:build": "pnpm --filter @xtarterize/docs build",
		"docs:preview": "pnpm --filter @xtarterize/docs preview",
		"up-interactive": "pnpm up -i -r",
		"up-latest": "pnpm up-interactive -L",
		"changeset": "changeset",
		"version-packages": "changeset version",
		"release": "changeset publish",
		"prepare": "vp config"
	},
	"devDependencies": {
		"@biomejs/biome": "catalog:",
		"@changesets/changelog-github": "^0.7.0",
		"@changesets/cli": "^2.31.0",
		"@types/node": "catalog:",
		"all-contributors-cli": "^6.26.1",
		"turbo": "^2.10.0",
		"vite-plus": "catalog:"
	},
	"devEngines": {
		"runtime": {
			"name": "node",
			"version": "24.16.0",
			"onFail": "download"
		}
	},
	"packageManager": "pnpm@11.9.0"
}
